# ============================================
# HandWood - Production Environment Config
# ============================================

# *** ОБЯЗАТЕЛЬНО ИЗМЕНИТЕ ВСЕ ЗНАЧЕНИЯ ПЕРЕД ДЕПЛОЕМ ***

# ============================================
# 🚀 SERVER CONFIGURATION
# ============================================
PORT=3000
NODE_ENV=production

# ============================================
# 🌐 SITE SETTINGS
# ============================================
# Полный URL вашего сайта (без слэша в конце)
SITE_URL=https://your-domain.com
SITE_TITLE=HandWood

# ============================================
# 📧 EMAIL / SMTP SETTINGS
# ============================================
# Для Hostinger:
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=587
SMTP_SECURE=false

# Gmail (если используете):
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false

# Email для отправки писем (может отличаться от ADMIN_EMAIL)
EMAIL_USER=your-email@your-domain.com
# Пароль приложения (НЕ основной пароль!)
EMAIL_PASS=your-app-specific-password

# Email администратора для уведомлений
ADMIN_EMAIL=admin@your-domain.com

# ============================================
# 🔐 SESSION & SECURITY
# ============================================
# ВАЖНО: Измените на случайную строку минимум 32 символа!
# Можно сгенерировать: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=change-me-to-random-32-char-string-abcdef123456789

# ============================================
# 📊 DATABASE
# ============================================
# SQLite3 путь (рекомендуется оставить как есть)
DATABASE_PATH=src/database.db

# ============================================
# 🔗 CORS / ORIGINS
# ============================================
# Разрешенные домены для CORS (через запятую)
CORS_ALLOWED=your-domain.com,www.your-domain.com,api.your-domain.com

# ============================================
# 🖼️ FILE UPLOADS
# ============================================
# Максимальный размер загрузки в MB
MAX_UPLOAD_SIZE=50

# ============================================
# 💾 PM2 (если используете PM2)
# ============================================
PM2_NAME=handwood-app

# ============================================
# 📝 LOGGING (опционально)
# ============================================
LOG_LEVEL=error
LOG_DIR=logs/

# ============================================
# 🌍 DEPLOYMENT OPTIONS
# ============================================
# Для PHP хостинга - укажите URL к Node.js приложению
# APP_URL=https://app.your-domain.com
# Или оставьте пустым если Node.js на том же хостинге
