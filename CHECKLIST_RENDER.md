# ✅ Чек-лист развертывания на Render.com

## 🎯 Что было сделано

- ✅ **Автоматическая инициализация админа** при запуске сервера
  - Если админ не существует - создается с данными из `ADMIN_USERNAME` и `ADMIN_PASSWORD`
  - Если админ уже существует - ничего не происходит
  - Работает при каждом запуске (безопасно)

- ✅ **Все файлы на GitHub** (репозиторий готов к деплою)
  - Включена папка `dist/` (нужна для Render)
  - Обновлен `.gitignore` (dist не исключена)
  - Обновлен `.env.example` с новыми переменными

- ✅ **Документация для Render**
  - Файл `RENDER_DEPLOYMENT.md` с пошаговыми инструкциями
  - Примеры переменных окружения
  - Решение проблем

## 🚀 Следующие шаги: Развертывание на Render

### Шаг 1: Откройте Render.com

```
https://render.com
```

### Шаг 2: Создайте новый Web Service

1. Нажмите **+ New**
2. Выберите **Web Service**
3. Соедините GitHub (если еще не подключили)
4. Выберите репозиторий: **hand-wood**
5. Нажмите **Connect**

### Шаг 3: Настройте параметры

| Параметр | Значение |
|----------|----------|
| Name | handwood-app |
| Environment | Node |
| Build Command | `npm install` |
| Start Command | `npm start` |

### Шаг 4: Добавьте переменные окружения (ВАЖНО!)

Перейдите в вкладку **Environment** и добавьте:

```
ADMIN_USERNAME = admin
ADMIN_PASSWORD = ваш-очень-надежный-пароль
SESSION_SECRET = ваша-случайная-строка-минимум-32-символа
NODE_ENV = production
SITE_URL = https://handwood-app.onrender.com
SMTP_HOST = smtp.hostinger.com
SMTP_PORT = 465
SMTP_SECURE = ssl
EMAIL_USER = ваш-email@domain.com
EMAIL_PASS = пароль-приложения
ADMIN_EMAIL = admin@domain.com
```

**Советы по безопасности:**
- `ADMIN_PASSWORD` минимум 12 символов (буквы + цифры + спецсимволы)
- `SESSION_SECRET` используйте генератор: https://randomkeygen.com/
- Для Gmail используйте пароль приложения, не основной пароль

### Шаг 5: Разверните

Нажмите кнопку **Deploy** → Render автоматически запустит процесс:

```
1. Клонирует репозиторий с GitHub
2. Установит зависимости (npm install)
3. Запустит сервер (npm start)
4. ✅ Создаст БД (автоматически)
5. ✅ Создаст админа (автоматически!)
```

Это займет 2-5 минут. Вы увидите логи процесса развертывания.

### Шаг 6: Проверьте что всё работает

После развертывания:

```
✅ Открыть: https://handwood-app.onrender.com
✅ Главная страница должна загрузиться
✅ Перейти на: https://handwood-app.onrender.com/admin
✅ Логин: admin
✅ Пароль: (значение ADMIN_PASSWORD)
```

Если видите сообщение вроде:
```
✅ Администратор "admin" уже существует
```

Значит всё работает правильно!

## 🔐 Первый вход в админ-панель

1. Откройте: `https://handwood-app.onrender.com/admin`
2. **Логин:** `admin` (или значение `ADMIN_USERNAME`)
3. **Пароль:** (значение `ADMIN_PASSWORD` из переменных окружения)

## ⚙️ После деплоя на Render

### Рекомендации:

1. **Измените пароль администратора**
   - Перейдите в админ-панель `/admin`
   - Найдите раздел "Смена пароля" или "Профиль"
   - Установите новый, более надежный пароль

2. **Обновите переменные окружения в Render**
   - Если вы изменили пароль - обновите `ADMIN_PASSWORD` в переменных
   - Это нужно для пересоздания админа если понадобится

3. **Протестируйте функции**
   - Добавьте категорию товаров
   - Добавьте товар с изображением
   - Проверьте работу формы заказа
   - Проверьте отправку email

4. **Настройте custom домен (опционально)**
   - Если у вас есть домен `handwood-md.com`
   - В Render → Settings → Custom Domains
   - Добавьте `handwood-md.com` или `app.handwood-md.com`

## 📊 Мониторинг приложения

В Render вы можете:
- 📋 **Логи** - см что происходит на сервере (вкладка Logs)
- 📈 **Метрики** - использование CPU, памяти (вкладка Metrics)
- 🔄 **Перезагрузка** - кнопка Reboot если что-то сломалось
- 🌐 **Custom домены** - привязать свой домен
- 📝 **Логирование** - логи доступны 24 часа

## 🆘 Если что-то не работает

### Проверьте логи на Render

1. Откройте ваш сервис на Render
2. Перейдите в **Logs**
3. Посмотрите последние строки
4. Ищите красные ошибки (ERROR)

### Типичные ошибки и решения

| Ошибка | Решение |
|--------|---------|
| `Cannot find module 'dotenv'` | БД не подключена, проверьте переменные окружения |
| `ADMIN_USERNAME not found` | Убедитесь что переменная окружения установлена |
| `Email send failed` | Проверьте SMTP параметры (хост, порт, пароль) |
| `Application failed to start` | Перезагрузитесь кнопкой Reboot или пересоздайте сервис |

### Для глубокой отладки

Если ничего не помогает - пересоздайте сервис:
1. **Settings** → **Delete Service**
2. Создайте новый Web Service с теми же параметрами
3. Render будет использовать новую копию с нуля

## 🎉 Успех!

Когда всё работает:

✅ Сайт доступен на `https://handwood-app.onrender.com`
✅ Админ-панель работает на `/admin`
✅ БД создана и содержит админа
✅ Email отправляется через SMTP
✅ Изображения загружаются

## 📚 Ссылки

- GitHub репозиторий: https://github.com/joxy9696666/hand-wood
- Render документация: https://render.com/docs
- Переменные окружения: https://render.com/docs/environment-variables
- Развертывание Node.js: https://render.com/docs/deploy-node-express-app

---

**Если у вас возникнут вопросы - обратитесь к файлу RENDER_DEPLOYMENT.md для более подробной информации.**
