# ✅ ГОТОВО К РАЗВЕРТЫВАНИЮ

## 🎯 Что сделано

Ваш проект **HandWood** полностью подготовлен к развертыванию на любом хостинге без необходимости Node.js.

### ✅ Выполнено

1. **Изучена архитектура проекта**
   - Express.js + Node.js SSR приложение
   - SQLite база данных
   - Admin панель с аутентификацией
   - REST API для работы с товарами и категориями
   - Загрузка и оптимизация изображений

2. **Создана система сборки**
   - Скрипт `npm run build` для создания папки `dist/`
   - Автоматическое копирование всех необходимых файлов
   - Генерация конфигов и документации

3. **Подготовлены для развертывания**
   - ✅ PHP адаптер (index.php) для проксирования запросов
   - ✅ PM2 конфигурация (ecosystem.config.js)
   - ✅ Environment переменные (.env.production)
   - ✅ Docker поддержка
   - ✅ Nginx конфиг (nginx.conf.example)
   - ✅ Systemd сервис (handwood.service.example)

4. **Создана документация**
   - ✅ QUICK_START.md - быстрый старт (5 минут)
   - ✅ ARCHITECTURE.md - подробное описание кода
   - ✅ DEPLOYMENT_HOSTING.md - 4 варианта развертывания
   - ✅ PROJECT_STRUCTURE.md - структура проекта
   - ✅ DEPLOY.md - находится в dist

---

## 🚀 Как развернуть

### Шаг 1: Собрать проект

```bash
npm run build
```

Создаст папку `dist/` со всем готовым для развертывания.

### Шаг 2: Выбрать вариант развертывания

#### 🌟 Вариант A: Railway.app (РЕКОМЕНДУЕТСЯ - самый простой)

```bash
# 1. Создайте аккаунт на railway.app
# 2. Свяжите GitHub репозиторий
# 3. Загрузите переменные окружения
# 4. Railway автоматически развернет приложение
```

**Результат:** https://handwood-app.railway.app

---

#### 🔧 Вариант B: PHP хостинг (Hostinger/Beget) + отдельный Node.js

```bash
# На PHP хостинге через FTP:
# 1. Загрузите содержимое dist/ в корень сайта
# 2. Отредактируйте .env файл
#    NODE_APP_URL=https://your-node-app.railway.app

# Node.js запустите отдельно на Railway/Render/DigitalOcean:
cd dist
npm install --production
npm run init-admin
npm start
```

---

#### 🐳 Вариант C: Docker (DigitalOcean/Hetzner/AWS)

```bash
docker build -t handwood .
docker run -d -p 3000:3000 handwood
```

---

#### 🖥️ Вариант D: VPS + Nginx (DigitalOcean/Hetzner)

```bash
# На VPS:
cd /var/www
cp -r dist handwood
cd handwood
npm install --production

# Используйте конфиги из корня проекта:
# - nginx.conf.example → /etc/nginx/sites-available/handwood
# - handwood.service.example → /etc/systemd/system/handwood.service

sudo systemctl start handwood
```

---

## 📁 Структура dist/

```
dist/
├── src/                    # Node.js приложение
├── public/                 # Статические файлы
├── api/                    # PHP скрипты
├── index.php              # ← PHP точка входа (проксирует на Node)
├── package.json           # ← Зависимости Node.js
├── .env.production        # ← Переменные окружения (шаблон)
├── .env.example           # ← Пример переменных
├── ecosystem.config.js    # ← PM2 конфиг
├── .gitignore             # ← Git исключения
└── DEPLOY.md              # ← Инструкции развертывания
```

---

## 📚 Документация

| Файл | Описание |
|------|---------|
| [QUICK_START.md](QUICK_START.md) | 🌟 **Начните отсюда** - за 5 минут |
| [ARCHITECTURE.md](ARCHITECTURE.md) | Подробная архитектура приложения |
| [DEPLOYMENT_HOSTING.md](DEPLOYMENT_HOSTING.md) | Все 4 варианта развертывания |
| [PROJECT_STRUCTURE.md](PROJECT_STRUCTURE.md) | Структура проекта и чек-листы |
| dist/DEPLOY.md | Инструкции для развертывания |

---

## 🔧 Новые команды

```bash
npm run build              # Собрать dist/ для развертывания
npm run build              # Запустить приложение локально (dev)
npm run prod               # Запустить в production режиме
npm run pm2:start          # Запустить через PM2
npm run pm2:stop           # Остановить PM2
npm run pm2:restart        # Перезагрузить PM2
npm run init-admin         # Создать администратора
```

---

## ⚙️ Переменные окружения

Основные переменные для .env:

```env
# Сервер
PORT=3000
NODE_ENV=production

# Сайт
SITE_URL=https://your-domain.com
SITE_TITLE=HandWood

# Email (SMTP)
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=587
EMAIL_USER=your@domain.com
EMAIL_PASS=your-password
ADMIN_EMAIL=admin@domain.com

# Безопасность
SESSION_SECRET=change-me-to-very-long-random-string

# Если используете PHP хостинг
NODE_APP_URL=https://your-node-app.railway.app
```

---

## 📋 Чек-лист перед развертыванием

- [ ] Выполнен `npm run build`
- [ ] Выбран хостинг (Railway.app рекомендуется)
- [ ] Загружены файлы из dist/
- [ ] Установлены зависимости: `npm install --production`
- [ ] Скопирован .env.production в .env
- [ ] Отредактированы все переменные окружения
- [ ] Создан администратор: `npm run init-admin`
- [ ] Приложение запущено
- [ ] Протестирована главная страница
- [ ] Протестирована админ-панель (localhost:3000/admin)
- [ ] Протестирована загрузка изображений
- [ ] Протестирована отправка писем

---

## 🆘 Если что-то не работает

### Приложение не запускается
```bash
npm start           # Проверьте консоль на ошибки
pm2 logs            # Если используете PM2
```

### БД не создается
```bash
chmod -R 755 src/   # Проверьте права доступа
npm run init-admin  # Пересоздайте админа
```

### Статические файлы не загружаются
```bash
chmod -R 755 public/            # Проверьте права
ls -la public/images/           # Проверьте структуру
```

### Email не отправляется
- Проверьте SMTP параметры в .env
- Используйте пароль приложения (не основной пароль)
- Проверьте что адрес EMAIL_USER правильный

---

## 💡 Рекомендация

**🏆 Начните с Railway.app:**
- Самый простой способ
- Бесплатный trial
- Автоматический deploy из GitHub
- Не нужно ничего настраивать на сервере

**Процесс:**
1. `npm run build`
2. Свяжите GitHub репозиторий с railway.app
3. Railway автоматически установит зависимости
4. Приложение будет доступно на https://your-app.railway.app

---

## 📞 Быстрые ссылки

- [Railway.app](https://railway.app) - рекомендуемый хостинг
- [Render.com](https://render.com) - альтернатива
- [DigitalOcean](https://www.digitalocean.com) - для VPS
- [Hetzner](https://www.hetzner.cloud) - недорогой VPS

---

## ✨ Итог

Ваш проект **полностью готов** к развертыванию на любом хостинге:
- ✅ Можно запустить на хостинге только с PHP
- ✅ Можно запустить на Node.js хостинге
- ✅ Можно запустить в Docker контейнере
- ✅ Можно запустить на обычном VPS

**Начните с [QUICK_START.md](QUICK_START.md) - займет максимум 5 минут!**

🎉 **Готово к развертыванию!**
