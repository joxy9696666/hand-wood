# 🚀 Быстрый старт - собрать и развернуть

## За 5 минут подготовить проект для загрузки на хостинг

### Шаг 1: Установите зависимости

```bash
npm install
```

### Шаг 2: Соберите проект в dist

```bash
npm run build
```

Это создаст папку `dist/` со всем необходимым для развертывания.

**Что находится в dist:**
- `src/` - приложение Node.js
- `public/` - статические файлы
- `index.php` - точка входа PHP для проксирования
- `package.json` - зависимости
- `DEPLOY.md` - подробные инструкции
- `.env.production` - конфиг для production

### Шаг 3: Готово! Содержимое dist можно загружать

```bash
# Локально протестируйте (опционально)
cd dist
npm install --production
cp .env.production .env
npm run init-admin  # создайте админа
npm start           # запустите на http://localhost:3000
```

---

## 📦 Пакетирование для хостинга

### Вариант A: Архив для загрузки

```bash
# Windows (PowerShell)
Compress-Archive -Path dist -DestinationPath handwood-app.zip

# Linux/Mac
zip -r handwood-app.zip dist/
tar -czf handwood-app.tar.gz dist/
```

### Вариант B: Git для автодеплоя

```bash
# Инициализируйте git в dist (если еще не инициализировали)
cd dist
git init
git add .
git commit -m "Initial deployment"
git remote add origin https://github.com/your/repo.git
git push -u origin main
```

---

## 🌐 Развертывание на хостинг

### Вариант 1: Railway.app (РЕКОМЕНДУЕТСЯ ДЛЯ БЫСТРОГО СТАРТА)

**Самый простой способ - 3 клика**

1. Перейдите на [railway.app](https://railway.app)
2. Нажмите "New Project" → "Deploy from Git"
3. Выберите ваш Git репозиторий
4. Railway автоматически установит зависимости и запустит

**Переменные окружения на Railway:**
- `PORT=3000`
- `NODE_ENV=production`
- `SITE_URL=https://your-app.railway.app` (автоматически)
- `SESSION_SECRET=your-random-string`
- `SMTP_HOST=smtp.hostinger.com`
- `EMAIL_USER=your@domain.com`
- `EMAIL_PASS=your-password`
- `ADMIN_EMAIL=admin@domain.com`

**Результат:** https://your-app-name.railway.app

---

### Вариант 2: Hostinger/Beget (PHP хостинг + отдельный Node.js)

1. **Загрузите dist на PHP хостинг через FTP:**
   ```
   Корень сайта:
   ├── index.php (из dist)
   ├── public/
   ├── src/
   ├── api/
   └── package.json
   ```

2. **Отредактируйте .env файл через FTP:**
   ```
   NODE_APP_URL=https://your-node-app.railway.app
   SESSION_SECRET=очень-длинная-случайная-строка
   # ... остальные настройки
   ```

3. **Запустите Node.js отдельно на Railway/Render/DigitalOcean**
   - Загрузите содержимое dist
   - Установите окружение
   - Запустите `npm start`

---

### Вариант 3: Docker (VPS/DigitalOcean/Hetzner)

1. **Создайте на VPS файл docker-compose.yml:**
   ```yaml
   version: '3.8'
   services:
     handwood:
       build: .
       ports:
         - "3000:3000"
       environment:
         NODE_ENV: production
         SESSION_SECRET: ${SESSION_SECRET}
         SMTP_HOST: smtp.hostinger.com
         EMAIL_USER: ${EMAIL_USER}
         EMAIL_PASS: ${EMAIL_PASS}
       volumes:
         - ./src/database.db:/app/src/database.db
       restart: always
   ```

2. **Запустите:**
   ```bash
   docker-compose up -d
   ```

---

## ✅ Чек-лист перед развертыванием

- [ ] Выполнен `npm run build`
- [ ] Содержимое dist готово
- [ ] Выбран хостинг
- [ ] Переменные окружения готовы
- [ ] `.env.production` отредактирован
- [ ] Проверены SMTP параметры
- [ ] Создан `SESSION_SECRET` (минимум 32 символа)
- [ ] Права на папки правильные
- [ ] HTTPS включен
- [ ] Протестировано локально

---

## 🔗 После развертывания

### Инициализируйте админа

На сервере выполните:
```bash
npm run init-admin
# Следуйте инструкциям для создания администратора
```

### Проверьте доступ

- **Главная:** https://your-domain.com
- **Админ-панель:** https://your-domain.com/admin
- **API:** https://your-domain.com/api/categories

### Настройте домен

Если используете Rails/Render/DigitalOcean, обновите DNS:
```
CNAME your-domain.com → your-app.railway.app
```

---

## 🆘 Что делать если что-то не работает?

### Приложение не запускается

```bash
# Проверьте логи
npm start

# Или через PM2
pm2 logs handwood-app

# Или через Docker
docker logs <container_id>
```

### БД не инициализируется

```bash
# Убедитесь что папка writable
chmod -R 755 src/
npm run init-admin
```

### Статические файлы не загружаются

```bash
# Проверьте структуру public/
ls -la public/
chmod -R 755 public/
```

### Email не отправляется

- Проверьте в .env: `SMTP_HOST`, `EMAIL_USER`, `EMAIL_PASS`
- Используйте пароль приложения (не основной пароль)
- Убедитесь что сервер может отправлять письма

---

## 📚 Дополнительно

- [ARCHITECTURE.md](ARCHITECTURE.md) - подробное описание кода
- [DEPLOYMENT_HOSTING.md](DEPLOYMENT_HOSTING.md) - все варианты развертывания
- [package.json](package.json) - зависимости и скрипты

---

## 💰 Рекомендуемые хостинги

| Хостинг | Цена | Сложность | Рекомендуется |
|---------|------|----------|--------------|
| Railway.app | $5-20/мес | Очень простая | ✅ Отлично |
| Render.com | $7-25/мес | Простая | ✅ Хорошо |
| DigitalOcean | $4-12/мес | Средняя | ✅ Хорошо |
| Hetzner Cloud | €3.99-39/мес | Средняя | ✅ Хорошо |
| AWS | Переменная | Сложная | ⚠️ Сложновато |
| Heroku | Отключен | - | ❌ Недоступен |

**🏆 Рекомендация:** Railway.app для быстрого старта, DigitalOcean для долгосрочного использования.

---

## 🎯 Итого

```
npm run build           → Собрать dist
Загрузить dist на хостинг
npm install --production → Установить зависимости
npm run init-admin      → Создать администратора
npm start               → Запустить приложение

Готово! 🎉
```
