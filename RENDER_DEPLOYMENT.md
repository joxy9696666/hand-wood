# 🚀 Развертывание на Render.com

## Проблема которую решаем
При деплое на Render БД и админ-пользователь инициализируются автоматически. **Не нужно вручную запускать npm run init-admin!**

## Как это работает

1. **При запуске сервера** (`npm start`)
2. **Автоматически проверяется** есть ли админ в БД
3. **Если админа нет** - создается новый с учетными данными из переменных окружения
4. **Если админ уже существует** - ничего не происходит

## Пошаговое развертывание на Render

### Шаг 1: Убедитесь что проект на GitHub

```bash
git push origin main
```

Репозиторий: https://github.com/joxy9696666/hand-wood

### Шаг 2: Создайте Web Service на Render.com

1. Зайдите на https://render.com
2. Нажмите **+ New**
3. Выберите **Web Service**
4. Соедините GitHub аккаунт
5. Выберите репозиторий: `hand-wood`
6. Нажмите **Connect**

### Шаг 3: Настройте параметры сервиса

| Параметр | Значение |
|----------|----------|
| **Name** | handwood-app |
| **Environment** | Node |
| **Build Command** | `npm install` |
| **Start Command** | `npm start` |
| **Plan** | Free или Paid (в зависимости от бюджета) |

### Шаг 4: Добавьте переменные окружения

Перейдите в **Environment** и добавьте:

```
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your-super-secure-password-123

SESSION_SECRET=generate-random-string-min-32-chars

NODE_ENV=production
PORT=3000
SITE_URL=https://handwood-app.onrender.com
SITE_TITLE=HandWood

SMTP_HOST=smtp.hostinger.com
SMTP_PORT=465
SMTP_SECURE=ssl
EMAIL_USER=your-email@domain.com
EMAIL_PASS=your-email-password
ADMIN_EMAIL=admin@domain.com
```

### Генерация безопасного SESSION_SECRET

**Используйте одну из команд:**

Linux/Mac:
```bash
openssl rand -base64 32
```

PowerShell (Windows):
```powershell
[Convert]::ToBase64String([System.Security.Cryptography.RNGCryptoServiceProvider]::new().GetBytes(32))
```

Или онлайн: https://randomkeygen.com/

### Шаг 5: Разверните

Нажмите кнопку **Deploy** в Render.

Render автоматически:
1. ✅ Установит зависимости (`npm install`)
2. ✅ Запустит сервер (`npm start`)
3. ✅ Создаст БД при первом запуске
4. ✅ **Автоматически создаст админа** с данными из `ADMIN_USERNAME` и `ADMIN_PASSWORD`
5. ✅ Выдаст URL: `https://handwood-app.onrender.com`

### Шаг 6: Войдите в админ-панель

1. Откройте: https://handwood-app.onrender.com/admin
2. **Логин:** значение `ADMIN_USERNAME` (по умолчанию: `admin`)
3. **Пароль:** значение `ADMIN_PASSWORD`

### Шаг 7: Измените пароль администратора

После первого входа:
1. Перейдите в админ-панель
2. Перейдите в **Профиль** или **Смена пароля**
3. **Установите новый пароль**
4. **Обновите** `ADMIN_PASSWORD` в переменных окружения Render

## 🔒 Безопасность

### ⚠️ ВАЖНО

1. **SESSION_SECRET** должен быть уникальным и длинным (минимум 32 символа)
2. **ADMIN_PASSWORD** должен быть сильным (минимум 12 символов, буквы+цифры+спецсимволы)
3. **EMAIL_PASS** - для Gmail используйте пароль приложения, не основной пароль
4. После первого входа измените пароль админа в админ-панели

### Переменные окружения для production

```
NODE_ENV=production          ← Обязательно!
SESSION_SECRET=<strong>      ← Обязательно, уникальное значение
ADMIN_PASSWORD=<strong>      ← Минимум 12 символов
EMAIL_PASS=<strong>          ← Пароль приложения для SMTP
```

## 🆘 Решение проблем

### БД не создается

**Симптомы:** Ошибка при открытии /admin

**Решение:**
1. Проверьте логи в Render: **Logs** → посмотрите последние сообщения
2. Убедитесь что `NODE_ENV=production`
3. Перезагрузите сервис: **Reboot** в Render

### Админ не создается

**Симптомы:** При входе /admin говорит "Неверные учетные данные"

**Решение:**
1. Проверьте что установлены переменные: `ADMIN_USERNAME`, `ADMIN_PASSWORD`
2. Посмотрите логи во время загрузки (должно быть сообщение "✅ Администратор успешно создан!")
3. Убедитесь что `ADMIN_USERNAME` не содержит пробелов
4. Перезагрузите сервис

### Email не отправляется

**Решение:**
1. Проверьте параметры SMTP в переменных окружения
2. Убедитесь что используете пароль приложения (для Gmail)
3. Проверьте что номер порта правильный (465 для SSL, 587 для TLS)

### Приложение падает при запуске

**Решение:**
1. Посмотрите логи (вкладка **Logs** в Render)
2. Проверьте что все переменные окружения установлены
3. Убедитесь что репозиторий на GitHub обновлен (`git push origin main`)

## 📊 Мониторинг

В Render.com вы можете:
- 📋 Смотреть логи: **Logs**
- 📈 Видеть использование ресурсов: **Metrics**
- 🔄 Перезагружать приложение: **Reboot**
- 🌐 Добавлять custom domains: **Settings** → **Custom Domains**

## 🔗 Подключение custom домена

Если у вас есть домен (например `handwood-md.com`):

### На Render:
1. Перейдите в **Settings**
2. Добавьте **Custom Domain**: `handwood-md.com`
3. Render даст вам CNAME запись

### На вашем регистраторе доменов:
1. Добавьте CNAME запись как указано в Render
2. Дождитесь распространения DNS (может быть 15-30 минут)

**Результат:** Сайт будет доступен по https://handwood-md.com вместо онrender.com

## 🔐 Дополнительно: Удаление админа в БД

Если нужно пересоздать админа:

```bash
# На вашей локальной машине
npm run init-admin

# Введите новые учетные данные
```

Или вручную через Render CLI:

```bash
# Подключитесь к Render
render shell

# Удалите существующего админа
sqlite3 src/database.db "DELETE FROM admins WHERE username='admin';"

# Выходите
exit
```

## 📚 Полезные ссылки

- [Render.com документация](https://render.com/docs)
- [Переменные окружения в Render](https://render.com/docs/environment-variables)
- [Node.js на Render](https://render.com/docs/deploy-node-express-app)

---

**Успешного развертывания! 🎉**
